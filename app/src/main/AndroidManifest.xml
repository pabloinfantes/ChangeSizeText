<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.changesizetext">

    <!-- En este ejercicio se ha creado una clase personalizada de Application
    para que el ejercicio funcione se debe añadir el atributo android:name y el nombre de la clase Application creada -->
    <application
        android:name=".ChangeSizeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChangeSizeText">
        <activity
            android:name=".ui.ViewMessageActivity"
            android:exported="true" />
        <activity
            android:name=".ui.ChangeSizeTextActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>